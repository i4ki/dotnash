# Setup environment variables
GOPATH = $HOME + "/workspace/go"
GOROOT = $HOME + "/go"
PATH   = $PATH + ":" + $GOPATH + "/bin:" + $GOROOT + "/bin"
SHELL  = "nash"

setenv GOPATH
setenv GOROOT
setenv PATH
setenv SHELL
